#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n,w;
    cin>>n>>w;
    int weights[n];
    int single_flag=0;
    int double_flag=0;
    int triple_flag=0;
    for (int i = 0; i < n; i++)
    {
        cin>>weights[i];
    }
    for (int j = 0; j < n; j++)
    {
        if (weights[j]<w)
        {
            
        }
        
    }
    
    

    
    
    return 0;
}