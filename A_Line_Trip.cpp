#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t,n;
    cin>>t;
    while(t--)
    {
        cin>>n;
        int gas_stations[n];
        for (int i = 0; i < n; i++)
        {
            cin>>gas_stations[i];
            
        }
        
    }
    
    
    return 0;
}