#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t,n;
    string s;
    cin>>t;
    for (int i = 0; i < t; i++)
    {
        cin>>n>>s;
        for (int i = 0; i < n; i++)
        {
            /* code */
        }
        
    }
    

    return 0;
}