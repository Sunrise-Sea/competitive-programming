#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t, n, even = 0;
    cin >> t;
    while (t--)
    {
        cin >> n;
        even = 0;
        int color[n];
        for (int i = 0; i < n; i++)
        {
        }
    }

    return 0;
}