#include <bits/stdc++.h>
// #define ll long long
// #define Loop(i, a, b) for (int i = a; i < b; i++)

using namespace std;

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int n, k;
    cin >> n >> k;
    int arrays[n], queries[k];
    for (int i = 0; i < n; i++)
    {
        cin >> arrays[i];
    }
    for (int j = 0; j < k; j++)
    {
        cin >> queries[j];
    }

    
    return 0;
}