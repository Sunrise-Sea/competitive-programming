#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t, n, max_blank = 0;
    cin >> t;
    while (t--)
    {
        max_blank = 0;
        cin >> n;
        bool dingus[n];
        for (int i = 0; i < n; i++)
        {
            cin >> dingus[i];
        }
        for (int ind = 0; ind < n - 1; ind++)
        {
        }
    }

    return 0;
}