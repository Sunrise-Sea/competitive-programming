#include <bits/stdc++.h>
// #define ll long long
#define rep(i, b) for (int i = 0; i < b; i++)
// since if we use O(m * n) complexity(two for loops and implementing simple linear search) it wouldn't work
// we can't really do anything about the m because we have to search m numbers
// can't sort because ideally we need to keep the array same and do minimum comparisons
// we can do some precomputation of the whole array and we can observe the index number(i +1) is the amount of comparisons (with the target)
// from the right it is i+1 and from the left it is n -i comparisons, done
using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;
    while(t--)
    {
        int n,m;
        cin>>n;
    }
    
    
    return 0;
}