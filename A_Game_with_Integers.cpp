#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t,n,move=1,remainder =0;
    cin>>t;
    for (int i = 0; i < t; i++)
    {
        cin>>n;
        remainder = n%3;
        if (remainder==0)
        {
            
        }
        
    }
    
    
    return 0;
}