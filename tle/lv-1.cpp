#include <iostream>

using namespace std;

int main()
{
    // in cpp expressions are calculated from left to right
    // for exathere are two expresssions in && then if the left condition is vilated then the right condition won't be evaluated
    // education rounds - mainly account for time taken to solve the problem
    // normal code forces rounds - mainly account for whether your solutions were wrong and based on that as a penalty you get 10 minutes added to your total time taken
    // for every 1 minute it costs 2 points on the max possible points scored
    //  both educationa and normal codeforces rounds 
    // accepted
    // wrong answer - answer didn't match
    /* 
    TLE - time limit exceeded = the solution exceeded allowed time limit for atleast one test case
    RTE - runtime error - the solution encountered an error during execution
    MLE - memory limit exceeded - the solution used more memory than allowed
    CE - Compilation error - the code failed to compile
    IE - idleness limit exceeded - the program performed no operations for a proloonged period of time
    skipped - the submission of skipped due to a higher priority submissio being judged ( common in contests)
    
    */
    return 0;
}