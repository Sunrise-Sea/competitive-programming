Searching is process of efficiently finding a solution or a specific piece of information
within a defined space
Defined space could be any data structure
What is the least value of n such that n^2>k hence now we don't have a range it could be upto infinity if k is such like that
The what in Searching
finding an element - does a specific value exist in a data set
optimization - what is the maximum minimum value that satisifies a condition
thresholds

linear search - it involves iterating through each element in the data set sequentially until the target avlue is found
or the entire data set is traversed

time complexity O(n) where n is the size of dataset
Space complexity : - O(1) since we don't take any space other than the array/dataset (only iterating on the dataset) and since it's n (constant) hence O(1)
can't implement on data structures that are not continuous memory block to work on (sequential memory/data)
non sequential eg trees graphs etc
works on both sorted and unsorted datasets

binary search
Question : given a sorted array 'a' of integers, answer 'q' queries of the nature: does qi number exist in the array
1<=n<=10^5
1<=a[i]<=10^5
1<=q<=10^5
1<=qi<=10^5
using linear search tc is O(n*q)--> TLE error since 10^10 maximum
since we need q we can't really optimize it hence we can optimize n
hence binary search is required --> it does that using the sorted array to it's advantage
hence we ask is the target number < or > the middle element
then we go to that half which may have the target (eg 14 is middle and target is 7 hence we go to the left half and never to the right half hence we decrease search space)
which makes it log2(n)(we are reducing search space by half each time) tc hence reducing tc
1 3 4 5 7 8 10 13 14 18 19 21 24 37 40 45 71
    hence we      ^    we never go to the right side
    are in this   |
   space        middle element
   similarly for all the rest of them recursively

and binary search is not just this

Maths checkpoint

predicate function - basically boolean only outputs for certain conditions

if the answer / target is monotonic predicate in nature then I can use binary search

minimize and maximize problems commonly include binary search
just see whether you can make a predicate function to it
eg n^2>20 then
1 2 3 4 5 6 7 8 
F F F F T T T T
hence see if 20 less than 6 now I have just 1 to 5 as search space
do it again and we get 5
binary searched right now

it is significantly faster than linear search in large data sets:
deterministic : - always provides the correct results for sorted data
cons:  -
the dataset is required to be sorted before hand
more complex to implement compared to lnear search 
not suitable for unsorted datasets without additional preprocessing

eg 
1 2 4 4 4 5 5 6 10 we want predicate set
F F T T T T T T T
because n>=4
this was the lower bound 

Selection sort
it repeatedly selects the smallest element from the unsorted part to the sorted part
20 12 10 15 2
now iterating we have 4 index (2) as min we take it and put it in the beginning
2 20 12 10 15
now repeat

Insertion sort
