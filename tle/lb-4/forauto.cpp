#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    string s = "akdsjf";
    for (auto ch : s){
        cout<<ch<<"\n";
    }
    // auto - automatically deduce the data type of the variable
    

    return 0;
}