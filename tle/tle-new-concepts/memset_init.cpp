#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // you can initiallize elements of an array with memset command
    int sample_array[8];
    memset(sample_array,0,sizeof(sample_array));
    for (int i = 0; i < 8; i++)
    {
        cout<<sample_array[i]<<"\n";
    }
    

    return 0;
}