// #include <iostream>// inbuilt file thing
#include <bits/stdc++.h> // we don't need to include anything else if we do this
// very good practice in cp (time saving)but not in actual code
// but it increases compile time but it doesn't matter for cp or code forces and doesn't work in other than GNU /C++
// #include <vector>
#define ll long long

using namespace std;

int main()
{

    // vector is not a part of the file iostream hence we include vector
    vector<int> v = {1, 2, 3, 4};

    return 0;
}