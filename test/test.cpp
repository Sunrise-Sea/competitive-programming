#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{

    /*     bool array[4];
        for (int i = 0; i < 4; i++)
        {
            cin >> array[i];
        }
        for (int i = 0; i < 4; i++)
        {
            cout << array[i];
        } */
    cout << abs(-1);

    return 0;
}